{
  "name": "todo-app-backend",
  "version": "1.0.0",
  "description": "Backend Lambda functions for serverless todo management application",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .js",
    "lint:fix": "eslint src --ext .js --fix"
  },
  "keywords": ["todo", "aws", "lambda", "dynamodb", "serverless"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.6.0",
    "eslint": "^8.45.0",
    "@eslint/js": "^8.45.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.370.0",
    "@aws-sdk/lib-dynamodb": "^3.370.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.370.0",
    "jsonwebtoken": "^9.0.1",
    "uuid": "^9.0.0"
  }
}