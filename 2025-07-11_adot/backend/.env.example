# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=adotdb
DB_USERNAME=adotuser
DB_PASSWORD=adotpass

# AWS Configuration (for production)
AWS_REGION=ap-northeast-1
# DB_SECRET_ARN=arn:aws:secretsmanager:region:account:secret:name

# Application Configuration
APP_NAME=ADOT Todo API
APP_VERSION=1.0.0
DEBUG=true

# Alternative: Direct database URL
# DATABASE_URL=postgresql+asyncpg://adotuser:adotpass@localhost:5432/adotdb